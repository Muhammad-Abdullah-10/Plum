{% case media.media_type %}
    {% when 'image' %}
        <img src="{{ media |  image_url: width: large  |  image_url: height: large}}" alt="{{ media.preview_image.alt }}" class=" w-full h-full object-cover">
    {% when 'video' %}
        
    {% else %}
        
{% endcase %}